# Student Score Application

## Project Structure

- `app.py` runs the demo application.
- `students.py` contains a list of students with names and scores.
- `utils/helpers.py` contains functions to find the highest and lowest scores.
- `requirements.txt` lists the project dependencies.

## How to Run

```bash
python app.py
```
